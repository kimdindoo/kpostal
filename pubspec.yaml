name: kpostal
description: Kpostal package can search for Korean postal addresses using Kakao postcode service.
version: 1.0.0
homepage: https://github.com/tykann/kpostal
publish_to: "none" # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # flutter_inappwebview:
  #   git:
  #     url: https://github.com/kimdindoo/flutter_inappwebview.git
  flutter_inappwebview: ^6.0.0

  geocoding: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

flutter:
  assets:
    - assets/kakao_postcode_localhost.html
